# Anonymous Function (Lambda)

## Problem

Use an anonymous (lambda) function to calculate the square of a number.

## Solution

```python
square = lambda x: x ** 2

num = int(input("Enter a number: "))
print(square(num))
```

## Sample Output

Input:
6

Output:
36

## Concepts Learned

- Lambda Functions
- Function Expressions
- User Input